
#[derive(Clone)]
pub struct AppState {
    // Exemplo de campos que você pode adicionar aqui futuramente
}

impl AppState {
    pub fn new() -> Self {
        Self {
            // Inicializações aqui
        }
    }
}
